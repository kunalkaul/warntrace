[project]
name = "transitive_dependency"
version = "0.1.0"
description = "Fixture: transitive dependency that emits a warning"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["uv_build>=0.11.22,<0.12.0"]
build-backend = "uv_build"
