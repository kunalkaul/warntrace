[project]
name = "direct_dependency"
version = "0.1.0"
description = "Fixture: direct dependency that emits a warning"
requires-python = ">=3.10"
dependencies = ["transitive_dependency"]

[build-system]
requires = ["uv_build>=0.11.22,<0.12.0"]
build-backend = "uv_build"
