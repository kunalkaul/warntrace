[project]
name = "sample-app"
version = "0.1.0"
description = "Fixture: sample app that depends on direct-dep"
requires-python = ">=3.10"
dependencies = ["direct-dep"]

[build-system]
requires = ["uv_build>=0.11.22,<0.12.0"]
build-backend = "uv_build"
